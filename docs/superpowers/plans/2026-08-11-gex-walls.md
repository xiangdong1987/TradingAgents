# GEX/墙位 + 方向标签 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `get_positioning` 升级为 30 天期权链聚合（GEX/Call墙/Put墙/聚合 P/C），日报每只美股加「期权面:」行与固定方向标签指示，期指块加 SPY 期权面行。

**Architecture:** `assistant/quotes.py` 新增 `_bs_gamma` 纯函数并重写 `get_positioning` 的期权段（多到期聚合、逐张合约算 Gamma 贡献、OI 聚合出墙位）；`assistant/daily_brief.py` 新增模块级 `_option_face(posi)` 格式化 helper，供每票行与 SPY 行共用，prompt 口径与结构指示追加期权面读法与四选一方向标签。runner/App 零改动。

**Tech Stack:** Python 3.12（repo `.venv`）+ pytest；math 标准库（Black-Scholes gamma，r=0）。

## Global Constraints

- 测试从仓库根目录：`source .venv/bin/activate && python -m pytest tests/<file> -q; echo RC=$?`
- 无新第三方依赖（`import math` 标准库）
- `get_positioning` 契约不变：至少一键才返回 dict、ISIN/.MI/.DE 直接 None、绝不抛出；现价缺失 → 期权键全部不写（做空键保留）
- GEX 公式：`Γ = φ(d1)/(S·σ·√T)`，`d1 = [ln(S/K)+0.5σ²T]/(σ√T)`（r=0）；贡献 = `Γ×OI×100×S²×0.01`，calls 正 / puts 负；`gexMUsd = round(Σ/1e6, 0)`，仅当至少一张合约实际计入时才写
- 聚合窗口：`0 ≤ 距到期天数 ≤ 30`，最多前 6 个到期；`T = max(天数/365, 1/365)`
- IV ≤ 0 或缺失的合约不计 GEX，但 OI 照常计入墙位与 P/C
- 直接提交本地 `main`，commit message 带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: `get_positioning` 期权段重写（quotes.py）

**Files:**
- Modify: `assistant/quotes.py`（顶部 `import math`；新增 `_bs_gamma`；`get_positioning` 签名与期权段）
- Test: `tests/test_assistant_quotes.py`（更新既有 `_FakeChain` 与 full_data 测试 + 新增测试，均模块顶层）

**Interfaces:**
- Consumes: 既有 `_FakeYfTicker`（tests 里已有：`info`/`options`/`option_chain(e)`）
- Produces（Task 2 依赖确切键名）: `get_positioning(ticker, *, _ticker_factory=None, _today: str | None = None) -> dict | None`，新增可选键 `gexMUsd`（float，百万$/1%）、`callWall`（float）、`putWall`（float）；`pcOi`/`pcVol` 变为跨到期聚合口径；`_today` 供测试注入（缺省取 UTC 今天）

- [ ] **Step 1: 改写既有 fixture 与测试 + 写新失败测试**

`tests/test_assistant_quotes.py`：

① `_FakeChain` 整体替换为带 strike/IV 的版本（旧签名 calls_oi/puts_oi/... 删除）：

```python
class _FakeChain:
    """rows: list of (strike, oi, volume, iv)。"""
    def __init__(self, calls, puts):
        import pandas as pd
        def df(rows):
            return pd.DataFrame({
                "strike": [r[0] for r in rows],
                "openInterest": [r[1] for r in rows],
                "volume": [r[2] for r in rows],
                "impliedVolatility": [r[3] for r in rows],
            })
        self.calls = df(calls)
        self.puts = df(puts)
```

② `test_positioning_full_data_hand_computed` 整体替换（手算：S=100、K=100、
σ=0.5、T=10/365 → Γ=0.0481632；call OI 100000 → +4.816M、put OI 200000
→ −9.633M；净 −4.816M → round 0 位 = −5.0）：

```python
def test_positioning_full_data_hand_computed():
    from assistant.quotes import get_positioning
    fake = _FakeYfTicker(
        info={"shortPercentOfFloat": 0.0139, "sharesShort": 108,
              "sharesShortPriorMonth": 100, "shortRatio": 2.24,
              "regularMarketPrice": 100.0},
        expiries=("2026-08-21",),
        chain=_FakeChain(calls=[(100.0, 100000, 10, 0.5)],
                         puts=[(100.0, 200000, 5, 0.5)]),
    )
    p = get_positioning("NVDA", _ticker_factory=lambda t: fake,
                        _today="2026-08-11")
    assert p == {"shortPctFloat": 1.39, "shortChangePct": 8.0,
                 "shortRatioDays": 2.24, "pcOi": 2.0, "pcVol": 0.5,
                 "gexMUsd": -5.0, "callWall": 100.0, "putWall": 100.0}
```

③ 末尾追加新测试：

```python
def test_bs_gamma_hand_computed():
    from assistant.quotes import _bs_gamma
    # S=100,K=100,σ=0.5,T=10/365 → Γ≈0.0481632（手算）
    assert abs(_bs_gamma(100.0, 100.0, 0.5, 10 / 365) - 0.0481632) < 1e-6
    assert _bs_gamma(100.0, 100.0, 0.0, 0.1) == 0.0     # IV 缺失
    assert _bs_gamma(100.0, 100.0, 0.5, 0.0) == 0.0     # 已到期


def test_positioning_walls_from_aggregated_oi():
    from assistant.quotes import get_positioning
    fake = _FakeYfTicker(
        info={"regularMarketPrice": 100.0},
        expiries=("2026-08-21",),
        chain=_FakeChain(calls=[(90.0, 10, 0, 0.5), (110.0, 50, 0, 0.5)],
                         puts=[(95.0, 70, 0, 0.5), (80.0, 20, 0, 0.5)]),
    )
    p = get_positioning("KO", _ticker_factory=lambda t: fake,
                        _today="2026-08-11")
    assert p["callWall"] == 110.0
    assert p["putWall"] == 95.0


def test_positioning_window_and_cap():
    from assistant.quotes import get_positioning
    chain = _FakeChain(calls=[(100.0, 100000, 0, 0.5)], puts=[])
    # 45 天外的到期被窗口排除：只剩一个 10 天的 → GEX = +5.0
    fake = _FakeYfTicker(info={"regularMarketPrice": 100.0},
                         expiries=("2026-08-21", "2026-09-25"), chain=chain)
    p = get_positioning("KO", _ticker_factory=lambda t: fake,
                        _today="2026-08-11")
    assert p["gexMUsd"] == 5.0
    # 窗口内 8 个同天数到期 → 只取前 6 个 → 6×4.816M ≈ 28.9 → 29.0
    fake8 = _FakeYfTicker(info={"regularMarketPrice": 100.0},
                          expiries=tuple(["2026-08-21"] * 8), chain=chain)
    p8 = get_positioning("KO", _ticker_factory=lambda t: fake8,
                         _today="2026-08-11")
    assert p8["gexMUsd"] == 29.0


def test_positioning_iv_missing_counts_walls_not_gex():
    from assistant.quotes import get_positioning
    fake = _FakeYfTicker(
        info={"regularMarketPrice": 100.0},
        expiries=("2026-08-21",),
        chain=_FakeChain(calls=[(110.0, 100000, 10, 0.0)], puts=[]),
    )
    p = get_positioning("KO", _ticker_factory=lambda t: fake,
                        _today="2026-08-11")
    assert p["callWall"] == 110.0
    assert "gexMUsd" not in p          # 没有任何合约计入 GEX 就不写
    assert p["pcOi"] == 0.0            # put 0 / call >0


def test_positioning_no_spot_drops_option_keys():
    from assistant.quotes import get_positioning
    fake = _FakeYfTicker(
        info={"shortRatio": 2.0},       # 无 regularMarketPrice/previousClose
        expiries=("2026-08-21",),
        chain=_FakeChain(calls=[(100.0, 100, 10, 0.5)], puts=[]),
    )
    p = get_positioning("KO", _ticker_factory=lambda t: fake,
                        _today="2026-08-11")
    assert p == {"shortRatioDays": 2.0}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
source .venv/bin/activate && python -m pytest tests/test_assistant_quotes.py -q -k "positioning or bs_gamma"; echo RC=$?
```

预期：FAIL/ERROR（`_bs_gamma` 不存在、`_FakeChain` 新签名、`_today` 未知参数）

- [ ] **Step 3: 实现**

`assistant/quotes.py`：

① 顶部加 `import math`（与既有 import 并列）。

② `get_positioning` 之前新增：

```python
def _bs_gamma(spot: float, strike: float, iv: float, t_years: float) -> float:
    """Black-Scholes gamma（r=0 简化）。参数非法一律 0，永不抛出。"""
    if iv <= 0 or t_years <= 0 or spot <= 0 or strike <= 0:
        return 0.0
    d1 = (math.log(spot / strike) + 0.5 * iv * iv * t_years) / (
        iv * math.sqrt(t_years))
    return math.exp(-0.5 * d1 * d1) / math.sqrt(2 * math.pi) / (
        spot * iv * math.sqrt(t_years))
```

③ `get_positioning` 签名改为
`def get_positioning(ticker: str, *, _ticker_factory=None, _today: str | None = None) -> dict | None:`
docstring 的期权句改为「30 天内到期链聚合的 put/call 比、净 Gamma 敞口
（GEX）与 Call/Put 墙」。

④ 期权段（原 `try: expiries = tk.options ...` 整段）替换为：

```python
        try:
            spot = info.get("regularMarketPrice") or info.get("previousClose")
            if spot:
                spot = float(spot)
                today_d = (datetime.strptime(_today, "%Y-%m-%d").date()
                           if _today else datetime.utcnow().date())
                expiries = []
                for e in tk.options or ():
                    try:
                        days = (datetime.strptime(e, "%Y-%m-%d").date()
                                - today_d).days
                    except ValueError:
                        continue
                    if 0 <= days <= 30:
                        expiries.append((e, days))
                expiries = expiries[:6]

                call_oi_total = put_oi_total = call_vol = put_vol = 0.0
                call_walls: dict[float, float] = {}
                put_walls: dict[float, float] = {}
                gex, gex_any = 0.0, False
                for e, days in expiries:
                    try:
                        chain = tk.option_chain(e)
                    except Exception:
                        continue          # 单到期失败跳过
                    t_years = max(days / 365, 1 / 365)
                    for df, sign, walls in ((chain.calls, 1, call_walls),
                                            (chain.puts, -1, put_walls)):
                        for _, row in df.iterrows():
                            oi = float(row.get("openInterest") or 0)
                            strike = float(row.get("strike") or 0)
                            if not (oi > 0 and strike > 0):
                                continue   # NaN 比较为 False，一并挡掉
                            walls[strike] = walls.get(strike, 0.0) + oi
                            g = _bs_gamma(spot, strike,
                                          float(row.get("impliedVolatility") or 0),
                                          t_years)
                            if g > 0:
                                gex += sign * g * oi * 100 * spot * spot * 0.01
                                gex_any = True
                        if sign > 0:
                            call_oi_total += float(df["openInterest"].sum())
                            call_vol += float(df["volume"].sum())
                        else:
                            put_oi_total += float(df["openInterest"].sum())
                            put_vol += float(df["volume"].sum())
                if call_oi_total > 0:
                    out["pcOi"] = round(put_oi_total / call_oi_total, 2)
                if call_vol > 0:
                    out["pcVol"] = round(put_vol / call_vol, 2)
                if gex_any:
                    out["gexMUsd"] = round(gex / 1e6, 0)
                if call_walls:
                    out["callWall"] = max(call_walls, key=call_walls.get)
                if put_walls:
                    out["putWall"] = max(put_walls, key=put_walls.get)
        except Exception:
            pass  # 期权链失败不拖累做空数据
```

- [ ] **Step 4: 跑测试确认通过**

```bash
source .venv/bin/activate && python -m pytest tests/test_assistant_quotes.py -q; echo RC=$?
```

预期：全绿，RC=0

- [ ] **Step 5: Commit**

```bash
git add assistant/quotes.py tests/test_assistant_quotes.py
git commit -m "feat(assistant): get_positioning 聚合 GEX/Call墙/Put墙

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 日报期权面行 + 方向标签 + SPY 行（daily_brief.py）

**Files:**
- Modify: `assistant/daily_brief.py`（`_option_face` helper、ticker 块、futures_block、`_PROMPT_TEMPLATE` 两处）
- Test: `tests/test_assistant_daily_brief.py`

**Interfaces:**
- Consumes: Task 1 的新键 `gexMUsd/callWall/putWall`（全可选）
- Produces: 每票（有数据时）多一行 `期权面: GEX -740M$（负Gamma）· Call墙 225 · Put墙 215`；期指块（SPY 有数据时）末尾多一行 `SPY 期权面: …`；prompt 含期权面口径与四选一方向标签指示

- [ ] **Step 1: 写失败测试**

`tests/test_assistant_daily_brief.py` 末尾追加（注入沿用文件既有全套假参数模式）：

```python
def _posi_gex(t):
    return {"gexMUsd": -740.0, "callWall": 225.0, "putWall": 215.0}


def test_brief_option_face_line_full():
    store, llm = make_store(), FakeLLM()
    generate_daily_brief(store, llm, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=_posi_gex, fetch_futures=lambda: [])
    prompt = llm.prompts[0]
    assert "期权面: GEX -740M$（负Gamma）· Call墙 225 · Put墙 215" in prompt


def test_brief_option_face_line_partial_and_absent():
    store, llm = make_store(), FakeLLM()
    generate_daily_brief(store, llm, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=lambda t: {"callWall": 210.0},
                         fetch_futures=lambda: [])
    assert "期权面: Call墙 210" in llm.prompts[0]

    store2, llm2 = make_store(), FakeLLM()
    generate_daily_brief(store2, llm2, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=lambda t: {"shortRatioDays": 2.0},
                         fetch_futures=lambda: [])
    assert "期权面:" not in llm2.prompts[0]     # 三键全缺不写此行


def test_brief_prompt_carries_gex_guidance_and_labels():
    store, llm = make_store(), FakeLLM()
    generate_daily_brief(store, llm, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=lambda t: None,
                         fetch_futures=lambda: [])
    prompt = llm.prompts[0]
    assert "GEX 为做市商净 Gamma 敞口" in prompt
    assert "【偏多】【偏空】【区间震荡】【高波动警惕】" in prompt
    assert "【数据不足】" in prompt


def test_brief_spy_option_face_in_futures_block():
    def posi(t):
        if t == "SPY":
            return {"gexMUsd": 1200.0, "callWall": 580.0, "putWall": 560.0}
        return None
    store, llm = make_store(), FakeLLM()
    generate_daily_brief(store, llm, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=posi, fetch_futures=lambda: [])
    assert "SPY 期权面: GEX +1200M$（正Gamma）· Call墙 580 · Put墙 560" in llm.prompts[0]


def test_brief_spy_face_absent_when_unavailable():
    store, llm = make_store(), FakeLLM()
    generate_daily_brief(store, llm, "2026-08-11",
                         fetch_quote=ok_quote, fetch_news=lambda t, s, e: "n",
                         fetch_money_flow=lambda t, d: None,
                         fetch_positioning=lambda t: None,
                         fetch_futures=lambda: [])
    assert "SPY 期权面" not in llm.prompts[0]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
source .venv/bin/activate && python -m pytest tests/test_assistant_daily_brief.py -q -k "option_face or gex_guidance or spy"; echo RC=$?
```

预期：FAIL（期权面行不存在）

- [ ] **Step 3: 实现**

`assistant/daily_brief.py`：

① 模块级（`_default_fetch_news` 之后）新增：

```python
def _option_face(posi: dict | None) -> str | None:
    """期权面片段：GEX/Call墙/Put墙 有几键写几段，全缺返回 None。"""
    if not posi:
        return None
    bits = []
    if "gexMUsd" in posi:
        tone = "正Gamma" if posi["gexMUsd"] >= 0 else "负Gamma"
        bits.append(f"GEX {posi['gexMUsd']:+.0f}M$（{tone}）")
    if "callWall" in posi:
        bits.append(f"Call墙 {posi['callWall']:g}")
    if "putWall" in posi:
        bits.append(f"Put墙 {posi['putWall']:g}")
    return " · ".join(bits) if bits else None
```

② ticker 块拼接处（现在的
`ticker_parts.append(f"## {t}{tag}\n{quote_line}\n{mf_line}\n{posi_line}\n近日新闻:\n{news}")`）
改为：

```python
        block = f"## {t}{tag}\n{quote_line}\n{mf_line}\n{posi_line}"
        face = _option_face(posi)
        if face:
            block += f"\n期权面: {face}"
        ticker_parts.append(block + f"\n近日新闻:\n{news}")
```

③ `futures_block` 构造之后加：

```python
    try:
        spy_face = _option_face(fetch_positioning("SPY"))
    except Exception:
        spy_face = None
    if spy_face:
        futures_block += f"\nSPY 期权面: {spy_face}"
```

④ `_PROMPT_TEMPLATE` 两处：

- 口径行（含「多空口径：…」的那行）在「做空数据为 FINRA 双周频，非实时。」
  之后插入：

  ```
  期权面口径：GEX 为做市商净 Gamma 敞口（正 = 对冲盘压波动、区间倾向；负 = 助涨助跌、易加速）；Call 墙为最大阻力位、Put 墙为最大支撑位；负 Gamma 且跌破 Put 墙是加速下行信号，数据为 30 天内期权链聚合。
  ```

- 结构行（`结构：## 组合概览…`）末尾追加一句：

  ```
  持仓点评与自选分析里，每只美股结尾给固定方向标签之一：【偏多】【偏空】【区间震荡】【高波动警惕】+ 一句理由（综合期权面、资金流、做空与新闻）；无期权数据的标的只按资金流与新闻给标签，依据不足时写【数据不足】，不要硬编。
  ```

- [ ] **Step 4: 跑测试确认通过**

```bash
source .venv/bin/activate && python -m pytest tests/test_assistant_daily_brief.py tests/test_assistant_runner.py tests/test_assistant_jobs.py -q; echo RC=$?
```

预期：全绿，RC=0

- [ ] **Step 5: Commit**

```bash
git add assistant/daily_brief.py tests/test_assistant_daily_brief.py
git commit -m "feat(assistant): 日报期权面行 + 方向标签 + SPY 大盘Gamma

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 真实验证 + 全量回归 + 收尾

**Files:**
- Modify: `docs/superpowers/specs/2026-08-11-gex-walls-design.md`（状态行 → `已落地（2026-08-11）`）

**Interfaces:**
- Consumes: Task 1/2 全部产出
- Produces: 无（验收）。runner 重启由控制器执行；App 无改动不部署。

- [ ] **Step 1: 真实 ticker 冒烟**

```bash
source .venv/bin/activate && python - <<'EOF'
from assistant.quotes import get_positioning
for t in ["NVDA", "KO", "SPY", "ENEL.MI"]:
    p = get_positioning(t)
    keys = {k: p.get(k) for k in ("gexMUsd", "callWall", "putWall", "pcOi")} if p else None
    print(t, keys)
EOF
```

预期：NVDA/SPY 三键齐全且数值合理（|GEX| 1~100000M、墙位在现价 ±50% 内）；
KO 至少有墙位；ENEL.MI 为 None。偶发网络失败重试一次，仍失败按 BLOCKED 报告。

- [ ] **Step 2: 真实 store 干跑 prompt（MemoryStore 收写、FakeLLM 截获，不写库不花钱）**

```bash
source .venv/bin/activate && python - <<'EOF'
from types import SimpleNamespace
from datetime import datetime, timezone
from assistant.store import FirestoreStore, MemoryStore
from assistant.daily_brief import generate_daily_brief

real = FirestoreStore.connect()
mem = MemoryStore()
mem.seed_watchlist(real.get_watchlist())
mem.seed_positions(real.get_positions())
mem.seed_meta(real.get_portfolio_meta())

class FakeLLM:
    def invoke(self, prompt):
        print(prompt[:5000])
        return SimpleNamespace(content="dry-run\n===EN===\ndry-run")

today = datetime.now(timezone.utc).strftime("%Y-%m-%d")
generate_daily_brief(mem, FakeLLM(), today, fetch_news=lambda t, s, e: "（略）")
EOF
```

预期：美股票有「期权面:」行；期指块有「SPY 期权面:」行；口径与标签指示在
prompt 里；意/德股与国债无期权面行。

- [ ] **Step 3: 全量回归**

```bash
source .venv/bin/activate && python -m pytest tests -q 2>&1 | tail -2; echo RC=$?
```

预期：全绿（≈815+ passed, 2 skipped），RC=0。App 无改动，Flutter 不跑。

- [ ] **Step 4: spec 状态 + Commit**

spec 状态行 `已确认，待实现` → `已落地（2026-08-11）`。

```bash
git add docs/superpowers/specs/2026-08-11-gex-walls-design.md
git commit -m "docs: 期权面spec标记已落地

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 5: runner 重启（控制器执行）**

`/runner stop` 再 `/runner`；实现者跳过本步。
